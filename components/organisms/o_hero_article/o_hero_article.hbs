<section class="o_hero_article"
	{{#if section_id}}
	 id="{{section_id}}"
	{{/if}}>
	<div class="container">
		{{> a_image }}
	</div>
	<div class="container-sm">
		{{> a_eyebrow }}
		{{> a_h1 }}
		{{> a_description }}
		{{> m_buttons }}
	</div>
</section>
